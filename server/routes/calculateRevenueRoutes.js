const express = require('express');
const router = express.Router();

// 수익 계산 관련 라우트 핸들러들
router.get('/', (req, res) => {
    // GET 요청 처리
});

router.post('/', (req, res) => {
    // POST 요청 처리
});

// 다른 라우트 핸들러들...

module.exports = router;  // 라우터를 내보내기
